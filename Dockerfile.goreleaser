FROM alpine:3.21

RUN apk add --no-cache git ca-certificates tzdata

COPY gclaw /usr/local/bin/gclaw

# Create default workspace directories
RUN mkdir -p /root/.gclaw/workspace

ENTRYPOINT ["/usr/local/bin/gclaw"]
